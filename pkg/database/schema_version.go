package database

// Versiones de esquema tenant (Migration System v2).
// V30 = baseline congelado: estado del sistema antes del registry incremental.
const (
	TenantSchemaBaselineVersion = 30
	TenantSchemaTargetVersion   = 38
)
